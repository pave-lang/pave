use std
use analyzer
use expression
use types

enum MatchPattern {
    Default,
    StaticValue(&Token),
    EnumVariant(&EnumVariant, Array<EnumVariantParameter>),
    EnumCValue(&EnumCValue),
    Expression(&Expression),
}
