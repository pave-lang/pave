use std
use analyzer
use expression
use types
use c

enum MatchPattern {
    Default,
    StaticValue(&Token),
    EnumVariant(&EnumVariant, Array<EnumVariantParameter>),
    EnumCValue(&EnumCValue),
    TypeId(Type),
}
