use std
use analyzer
use expression
use types
use c

enum MatchPattern {
    Default,
    StaticValue(&Expression),
    EnumVariant(&Type, &EnumVariant, Array<EnumVariantParameter>),
    EnumCValue(&EnumCValue),
    TypeId(Type),
}
