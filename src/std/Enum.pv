struct EnumVariantInfo {
    index: usize,
    key: str,
    label: str,
}

trait Enum {
    fn get_variants(&self) -> &[EnumVariantInfo];
    fn get_variant_index(&self) -> usize;
    fn set_variant(&self, index: usize);
}
