trait Add<T> {
    fn add(&self, other: T) -> void;
}

trait Sub<T> {
    fn sub(&self, other: T) -> void;
}

trait Mul<T> {
    fn mul(&self, other: T) -> void;
}

trait Div<T> {
    fn div(&self, other: T) -> void;
}
