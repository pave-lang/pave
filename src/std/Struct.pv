struct DynStructField {
    name: str,
    typeid: TypeId,
}

trait Struct {
    fn get_fields(&self) -> &[DynStructField];
    fn get_field(&self, index: usize) -> &void;
    fn set_field(&self, index: usize, value: *void) -> bool;
}
