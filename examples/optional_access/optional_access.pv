#include "stdio.h"
#include "stdlib.h"

struct Asset {
    value: i32,
}

impl Asset {
    fn data(&self) -> &i32 {
        let static DATA: i32 = 42;
        return &DATA;
    }
}

fn main(argc: i32, argv: **const char) -> i32 {
    let asset: *Asset = null;

    let next_asset = asset?.data();

    return 0;
}
