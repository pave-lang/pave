#include "stdio.h"
#include "stdlib.h"

struct Asset {
    value: i32,
}

impl Asset {
    fn data(&self) -> &i32 {
        let static DATA: i32 = 42;
        return &DATA;
    }

    fn next(&self) -> *Asset {
        return null;
    }

    fn next2(&self) -> *Asset {
        return null;
    }
}

fn main(argc: i32, argv: **const char) -> i32 {
    let asset: *Asset = null;

    let data = asset?.data();
    let next_data = asset?.next()?.data();
    let next_next2_data = asset?.next()?.next2()?.data();

    return 0;
}
